<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
  <mapper namespace="com.quiz.lesson05.weather_history.mapper.WeatherHistoryMapper">
  
  	<select id="selectWeatherHistory" resultType="com.quiz.lesson05.weather_history.domain.WeatherHistory">
  	SELECT
  		`id`
		,`date`
		,`weather`
		,`temperatures`
		,`precipitation`
		,`microDust`
		,`windSpeed`
		,`createdAt`
		,`updatedAt`
  	FROM
  		`weatherhistory`;
  	</select>
  	
  	<insert>
  	INSERT INTO `weatherhistory`
  	(
  		`date`
		,`weather`
		,`temperatures`
		,`precipitation`
		,`microDust`
		,`windSpeed`
		,`createdAt`
		,`updatedAt`
  	)
  	VALUES
  	(
  		#{date}
		,#{date}
		,#{date}
		,#{date}
		,#{date}
		,#{date}
		, NOW()
		, NOW()
  	)
  	</insert>
  </mapper>